name: Android Test CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      # 1. Lấy code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Java
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3. Cache Gradle
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*') }}

      # 4. Cấp quyền gradlew
      - name: Grant execute permission
        run: chmod +x gradlew

      # 5. Set Gradle memory (tránh OOM)
      - name: Set Gradle Memory
        run: echo "org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m" >> gradle.properties

      # 6. Chạy Unit Test
      - name: Run Unit Tests
        run: ./gradlew test

      # 7. Restart ADB (tránh offline)
      - name: Restart ADB
        run: |
          adb kill-server
          adb start-server

      # 8. Chạy UI Test
      - name: Run UI Tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 26
          target: default
          arch: x86_64
          disable-animations: true
          emulator-options: >
            -no-window
            -no-audio
            -no-boot-anim
            -no-snapshot-save
            -memory 2048
            -partition-size 2048
          emulator-boot-timeout: 600
          script: |
            ./gradlew assembleDebug assembleAndroidTest
            ./gradlew connectedAndroidTest
