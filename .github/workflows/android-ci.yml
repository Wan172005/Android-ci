name: Android Test CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      # 1. Checkout
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Java
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3. Setup Android SDK (QUAN TRỌNG)
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 4. Cache Gradle
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*') }}

      # 5. Grant permission
      - name: Grant execute permission
        run: chmod +x gradlew

      # 6. Unit Test
      - name: Run Unit Tests
        run: ./gradlew test

      # 7. Restart ADB (sau khi có SDK)
      - name: Restart ADB
        run: |
          adb kill-server
          adb start-server

      # 8. Đợi emulator ready thật sự
      - name: Wait for emulator ready
        run: |
          adb wait-for-device
          adb shell 'while [[ -z $(getprop sys.boot_completed) ]]; do sleep 2; done;'
          adb shell 'while [[ -z $(getprop init.svc.bootanim | grep stopped) ]]; do sleep 2; done;'
          sleep 30

      # 9. Disable split apk
      - name: Disable Split APK
        run: echo "android.injected.enableSplitApkInstall=false" >> gradle.properties

      # 10. Run UI Test
      - name: Run UI Tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: default
          arch: x86_64
          disable-animations: true
          emulator-options: >
            -no-window
            -no-audio
            -no-boot-anim
            -no-snapshot-save
            -memory 2048
          emulator-boot-timeout: 900
          script: |
            ./gradlew assembleDebug assembleAndroidTest
            ./gradlew connectedAndroidTest

